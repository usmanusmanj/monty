#include "monty.h"
/**
 * f_stack – it prints the top
 * @head: the stack head
 * @counter: the line_number
 * Return: no return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
